# GD Workflow Backend Configuration
# Set these environment variables to connect to your GD Workflow backend server

# Auth Service endpoint (runs on port 3002)
VITE_LICENSE_SERVER_URL=http://localhost:3002

# Optional: API Gateway endpoint (for direct API calls)
VITE_API_URL=http://localhost:3000/api/v1
